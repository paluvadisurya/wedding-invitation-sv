Drop the couple's 3 photos in this folder with these exact names:

  hero.jpg    → the hero portrait (used at the top of the site; portrait orientation, ~1200px tall works best)
  story.jpg   → woven into the "Our story" scene (portrait orientation)
  finale.jpg  → shown in the gallery finale

JPG, PNG or WebP all work — if you use a different extension or name,
update the paths under "photos" in config.json.

The site shows elegant "photo coming soon" placeholders until these
files exist, so nothing ever looks broken.

Tip: compress photos before uploading (e.g. squoosh.app) — aim for
under ~350 KB each so the invitation stays fast on mobile data.
